name: Finance API CI

on:
  push:
    branches: [ "main", "develop" ]
    paths:
      - 'ms-finance-api/**' # SÓ roda se houver mudanças nesta pasta
  pull_request:
    branches: [ "main" ]
    paths:
      - 'ms-finance-api/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o código do repositório
        uses: actions/checkout@v4

      - name: Configurar o Java JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Compilar e Rodar Testes com Maven
        run: mvn clean test
        working-directory: ./ms-finance-api # Entra na pasta do finance antes de rodar o comando